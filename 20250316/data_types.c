#include <stdio.h>

int main() {
    int a = 14;
    int b = 22;
    int c = 45;

    char q = 'a';
    printf("%c",q);
    int op = a * b / c;
    printf("the output of operation is %d", op);
    return 0;
}